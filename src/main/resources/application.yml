server:
  port: 8080

spring:
  profiles:
    active: dev
  main:
    allow-circular-references: true
  datasource:
    druid:
      driver-class-name: ${rag.datasource.driver-class-name}
      url: jdbc:mysql://${rag.datasource.host}:${rag.datasource.port}/${rag.datasource.database}?serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieval=true
      username: ${rag.datasource.username}
      password: ${rag.datasource.password}
  redis:
    host: ${rag.redis.host}
    port: ${rag.redis.port}
#    password: ${sky.redis.password}
    database: ${rag.redis.database}
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB
  mail:
    host: smtp.qq.com
    port: 465
    username: 1073931273@qq.com
    password: wpkefohqoljjbaeh
    default-encoding: UTF-8
    properties:
      mail:
        debug: true
        socketFactory:
          class: javax.net.ssl.SSLSocketFactory

mybatis:
  #mapper配置文件
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.vichayturen.rag_chatman.entity
  configuration:
    #开启驼峰命名
    map-underscore-to-camel-case: true

logging:
  level:
    com:
      sky:
        mapper: debug
        service: info
        controller: info

rag:
  jwt:
    # 设置jwt签名加密时使用的秘钥
    admin-secret-key: itcast
    # 设置jwt过期时间
    admin-ttl: 7200000
    # 设置前端传递过来的令牌名称
    admin-token-name: token
    user-secret-key: itheima
    user-ttl: 7200000
    user-token-name: authentication
  llm:
    selected: zhipu
    zhipu:
      api-key: f8d197a3630fbba06bdccbb5f36340fe.qPuVwL0zPHsDOC8S
      model: glm-4
      url: https://open.bigmodel.cn/api/paas/v4/chat/completions
  lucene:
    path: ${rag.lucene.path}
